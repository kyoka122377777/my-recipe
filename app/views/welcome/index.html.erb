<!DOCTYPE html>
<html>
<head>
  <title><%= t('welcome.title') %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', type: 'module' %>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <%= javascript_importmap_tags %>
</head>
<body>
  <main class="container text-center mt-5">
    <div class="welcome-main">
      <h1 class="welcome-text"><%= t('welcome.title') %></h1>
      <p class="welcome-text"><%= t('welcome.subtitle') %></p>
      <div class="button-container mt-4">
        <%= link_to t('welcome.new_user'), new_user_path, class: "btn btn-primary me-2" %>
        <%= link_to t('welcome.login'), login_path, class: "btn btn-secondary" %>
      </div>
    </div>

    <%= image_tag "top.png", alt: "welcomeイメージ", class: 'top-image img-fluid mt-4' %>

  </main>
</body>
</html>
